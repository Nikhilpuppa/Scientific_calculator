---
- name: Pull Docker Image of Calculator
  hosts: all
  vars:
    ansible_python_interpreter: /usr/bin/python3
  tasks:
    - name: Pull Docker image
      docker_image:
        name: chandranikhil/scientific_calculator:latest
        source: pull

    - name: Start Docker service
      service:
        name: docker
        state: started

    - name: Run Docker container
      shell: docker run -it -d --name Calculator chandranikhil/scientific_calculator
